<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.sevan.sjlibrary">

    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".imagepicker.view.ImagePickerActivity"
            android:label="@string/image_picker" >
        </activity>
        <activity
            android:name=".imagepicker.view.ImagePreviewActivity"
            android:label="@string/image_preview" >
        </activity>
    </application>

</manifest>
